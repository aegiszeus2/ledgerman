BACKUP ENCRYPTION PASSWORD
============================
To decrypt backups, use:
  openssl enc -d -aes-256-cbc -in BACKUP_FILE.zip.enc -out BACKUP_FILE.zip -k "PASSWORD"

Password is stored in: /home/lucaspc3/Desktop/Project Organizer/ledgeman-backups/.backup_password (excluded from git)
Keep this file secure — it's the only key to restore encrypted backups.
